// FILE: lib/services/scoring_service.dart
// PURPOSE: Compare transcript to target and compute a 0–100 score (stub == exact match).
// TOOLS: Pure Dart; later can use fuzzy/phoneme distance.
// RELATIONSHIPS: Called by PracticeModel after STT; result saved by attempts_repository.dart → surfaced in Progress/Teacher views.
